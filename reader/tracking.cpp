#include "tracking.h"

tracking::tracking()
{
	
}

void tracking::newTracking(std::string path, int seconds)
{
//	boost::thread *t = new boost::thread(&myThred);
//	threads.push_back(t);
}

void tracking::wait(int sec)
{
//	boost::this_thread::sleep_for(boost::chrono::seconds(sec));
}

void tracking::myThred()
{

}

tracking::~tracking()
{
}